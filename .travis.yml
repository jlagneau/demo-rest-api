language: php

php:
  - 5.6
  - 7
  - hhvm

branches:
  only:
      - master
      - develop

before_script:
  - composer self-update
  - composer install --no-interaction --prefer-source

script: php bin/phpunit --coverage-clover build/logs/clover.xml

after_script:
  - sh -c 'if [ "$TRAVIS_PHP_VERSION" != "hhvm" ]; then php bin/coveralls -v; fi;'
